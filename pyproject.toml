[project]
name = "python_ai"
version = "0.1.0"
description = "This environment created for python learning for agentic ai course"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
]
